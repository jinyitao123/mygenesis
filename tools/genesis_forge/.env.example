# Genesis Forge Studio 环境配置示例
# 复制此文件为 .env 并修改相应配置

# ========== 应用配置 ==========
# 应用运行环境: development, production, testing
APP_ENV=development

# 应用调试模式
DEBUG=true

# 应用密钥 (用于会话加密等)
SECRET_KEY=your-secret-key-here-change-in-production

# 应用主机和端口
HOST=0.0.0.0
PORT=5000

# ========== 数据库配置 ==========
# Neo4j 图数据库配置
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
NEO4J_DATABASE=neo4j

# 数据库连接池配置
NEO4J_MAX_CONNECTION_POOL_SIZE=50
NEO4J_MAX_CONNECTION_LIFETIME=3600  # 秒

# ========== 前端配置 ==========
# Vue 前端开发服务器
VITE_APP_TITLE=Genesis Forge Studio
VITE_API_BASE_URL=http://localhost:5000/api

# ========== 日志配置 ==========
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=logs/genesis_forge.log

# 日志格式
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ========== 文件存储配置 ==========
# 上传文件大小限制 (MB)
MAX_CONTENT_LENGTH=16

# 临时文件目录
TEMP_DIR=temp

# ========== 安全配置 ==========
# CORS 配置
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# 会话配置
SESSION_COOKIE_SECURE=false  # 生产环境应为 true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# ========== 性能配置 ==========
# 缓存配置
CACHE_TYPE=simple  # simple, redis, filesystem
CACHE_DEFAULT_TIMEOUT=300  # 秒

# 异步任务配置 (如果使用 Celery)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ========== AI 服务配置 ==========
# OpenAI API 配置 (用于 AI Copilot)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=1000

# ========== Git 配置 ==========
# Git 仓库配置
GIT_AUTO_COMMIT=true
GIT_DEFAULT_BRANCH=main

# ========== 测试配置 ==========
# 测试数据库配置
TEST_NEO4J_URI=bolt://localhost:7688
TEST_NEO4J_USER=neo4j
TEST_NEO4J_PASSWORD=testpassword

# 测试模式
TEST_MODE=false